[build]
dockerfilePath = "movo-backend/Dockerfile"
buildCommand = ""

[deploy]
startCommand = ""
healthcheckPath = "/api/auth/sync"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
